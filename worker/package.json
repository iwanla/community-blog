{
  "name": "jelajah-blog-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.dev.jsonc",
    "deploy": "wrangler deploy --config ../wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply jelajah_blog --local --config ../wrangler.dev.jsonc",
    "db:migrate:remote": "wrangler d1 migrations apply jelajah_blog --remote --config ../wrangler.jsonc"
  },
  "dependencies": {
    "hono": "^4.6.17"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.92.0"
  }
}
